import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFFFF7F34);
  static const Color primaryLight = Color(0xFFFFAA72);
  static const Color primaryDark = Color(0xFFCC5F1A);

  // Accent
  static const Color accent = Color(0xFF1A1A1A);

  // Background
  static const Color backgroundLight = Color(0xFFF5F5F5);
  static const Color backgroundDark = Color(0xFF121212);

  // Surface
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF1E1E1E);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF565656);
  static const Color textDisabled = Color(0xFFBDBDBD);
  static const Color textWhite = Color(0xFFFFFFFF);

  // Status
  static const Color success = Color(0xFF2E7D32);
  static const Color error = Color(0xFFC62828);
  static const Color warning = Color(0xFFE65100);
  static const Color info = Color(0xFF1565C0);

  // Border
  static const Color border = Color(0xFFE0E0E0);
  static const Color borderDark = Color(0xFF2C2C2C);

  // Divider
  static const Color divider = Color(0xFFEEEEEE);

  // Transparent
  static const Color transparent = Colors.transparent;
}
